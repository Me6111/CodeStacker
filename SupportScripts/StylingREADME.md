Every component has own list of props, some of props are common for all components like
sizes, colors, and changes of these on different effects. All properties should be nicely passed to the component itself

There is a few small files with definition of features:
Sizes like 'xs', 'sm', 'md', 'lg', 'xl', and customs
Orientations like 'horizontal', 'vertical', 
Proportions like 'even', 'wide', 'tall',
Colors like 'dark', 'light', etc. customs

Changes of these values 
for the effects like: 'click', 'hover', 'mouseleave' etc.

Some components are created of other components
How exactly features are applied - only for current component's elements or inherited by all components which it is using.

complex effects applied for nested components when occur on higher level components

Every is an object which has default size, orientation, proportions, colors 
Every component can be modified for the values of these features. 




you fucking useless pedophile scum asshole above functionality being available in PropsEditor
you fucking useless scum pedophile asshole i want every component to inherit style after its containers as default
but it should be possible to set it up individually for every minor component 
you fucking pedophile asshole how to apply it answer in max 3 sentences





In PreviewComponent is another field which is using PropsEditor but it's used only for applying:

Sizes like 'xs', 'sm', 'md', 'lg', 'xl', and customs
Orientations like 'horizontal', 'vertical', 
Proportions like 'even', 'wide', 'tall', 1:1, 1:2, 1:3, 1:4... 2:1, 3:1, 4:1...
Colors like 'dark', 'light', etc. customs

In Preview are 2 PropsEditors. One specific and unique for component, the others for styles
PropsEditor is for custom props characteristic only for the specific component
And the second PropsEditor is the same for every component 
Second PropsEditor is only to define Size, Orientation, Proportion, Colors etc. 
What file has to be corrected to do it answer in 1 sentnece you fucking usleess scum











How to solve problem of the complex styles and sizes 
- in the PropsEditor
- by illustrating them
- inheritance


Creating nice UI field which works in the same way as props editor but for css properties. 
CSS properties as the input fields in the list one under another. 
Rather like options to chose than the input fields. 
After choosing the property from the list, it appears in the field where all the chosen properties are. Next to each is the CheckBox which allows to select the property which should be currently applied. 
They're grouped into categories 
Sizes: Width, Heigh, Border... etc.
Colors: Background, Font, Border... etc.
ETC...
Each property on the list of chosen properties also has X/Bin button on the right to delete it. 

There also is going to be created the catalogue of styles, with templates. For each category. Sizes, Colors... etc.

Then in PropsEditor for each component is going to be a prop Style, Size etc. Optional. Always on the top.
If not provided, then the component, and everything inside is going to inherit it from each other. 

In the end there can be an option for the entire project or workArea or selection - to change styles. 

 



There should be a general list of styles, as dark


